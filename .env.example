# Producción / Docker
PORT=4000
SESSION_SECRET=replace-with-a-long-random-secret

# Solo se usan para crear el admin inicial si no existe en la DB
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-a-strong-initial-password

# Opcional
TZ=Europe/Madrid
